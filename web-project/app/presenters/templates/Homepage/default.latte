{block content}
    <h1 n:block="title">Můj blog</h1>

    <div n:foreach="$posts as $post" class="post">
    	<div class="date">{$post->created_at|date:'F j, Y'}</div>

    	<h2>{$post->title}</h2>

    	<div>{$post->content}</div>
	</div>
{/block}

{block testlatte}
<h3>Foreach test</h3>
{var $words = ['Latte', 'test', 'template']}

<p n:foreach="$words as $word">{$word|upper}</p>

{/block}